//npm install prompt-sync

const prompt = require("prompt-sync")({ sigint: true });


var arr = [];                               // define our array

for (var i = 0; i < 3; i++) {              // loop 10 times
  arr.push(prompt('Enter grade ')); // push the value into the array
}



for (array of data){
    console.log(array);
}
